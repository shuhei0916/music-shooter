# 引継ぎ指令書

## 現況サマリ
- `Main` は StartTimer が鳴るまで `world_speed=0`。`_on_start_timer_timeout` で `world_speed=5.0` にし、Spawner/SpawnTimer を再開、MidiPlayer を再生するフロー。カウントダウン表示は未実装。
- GameOver/完奏で `GameUI.show_result` を呼び、`world_speed=0` と Spawner 停止、Midi 停止まで行う。`ui_accept`（Enter/Space）でシーン再読み込み。
- Spawner: 5回に1回 Gate 行を生成（Gate値は add 5〜20のランダム）。それ以外の回に `enemy_spawn_probability=0.1` で敵をスポーン。敵HPは 5〜20 ランダム。敵/ゲートは AnchorRoot のマーカー位置に配置。
- Player: 敵衝突時にHP差分処理（勝ち/負け/引き分け）を実装済み。`shoot()` は `bullet_scene` を親（`bullet_container`=親ノード）に直接追加するシンプル版。弾丸は `Bullet` シーン（クラス名付与済）で敵命中時に `take_damage(1)`。
- テスト: `godot --headless --script addons/gut/gut_cmdln.gd -gdir=res://tests/ -gexit` が Green だった時点からリセット済。StartTimer・リザルト・衝突系など一通り通る。

## 未コミット/注意点
- `scenes/ui/components/growth_curve_graph.tscn` が未追跡で残っている。`scenes/ui/debug_midi_ui.gd` が変更あり。必要に応じて整理すること。
- リポジトリは `origin/refactor/min-core` より1コミット behind。

## これからの優先タスク（todo.md反映）
- スタートタイマーを拍子に合わせたカウントイン（例: 3/4→1,2,3,1,2,3）。MIDIの拍子メタイベント読み取り→StartTimer wait_time=秒/拍→F2表示不要なら SongProgressLabel に数字表示。
- GameUI と debug_midi_ui の統合：F2トグルで DebugPanel を表示し、トラック別ノートカウントと GrowthCurve グラフを表示。`note_counts` を `_on_midi_event` で更新して渡す。
- MIDIノート数表示（HUD or Debug）を追加。
- ゲート山札方式・レアリティ配色（未着手）。
- 敵当たり判定拡大、演出系（確定演出、HP変化エフェクトなど）。

## テストコマンド
```
godot --headless --script addons/gut/gut_cmdln.gd -gdir=res://tests/ -gexit
```

## 小ネタ/過去実装メモ
- `debug_toggle` アクションは Project Settings で F2 割当済みだが、現在 Main の `_unhandled_input` にはトグル処理なし（リセット後）。統合時に再利用すると良い。
- `GameUI` の SongProgressLabel をカウントダウン表示に流用する案が既に一度試されたがリセット済。再導入時は `ceil(time_left)` で3→2→1、0以下でクリアする方式がシンプル。
